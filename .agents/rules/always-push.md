# Always Push Rule

Whenever changes, features, or bug fixes are completed and committed, automatically push them to the remote repository (`git push origin master`) without requiring the user to explicitly ask.
